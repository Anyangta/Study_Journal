# 2노드 실험 (C1) — cluster03 복구 후

*2026-09-09 17:10 작성. cluster03 재부팅·복구 완료 시점.*

## cluster03 복구 확인 (2026-09-09)

- **재부팅**: 16:06 (사용자가 물리 전원). 부팅 후 sshd가 늦게 떠서 초기엔 5408 닫힘 → 이후 정상
- **원인**: dmesg에 I/O 오류 0건. 디스크 readonly 아님, 여유 1.4 TB. **하드웨어 고장 아님** —
  일시적 소프트웨어/커널 행으로 추정 (persistent journal이 직전 부팅을 다 못 남김)
- **sshd 포트**: cluster03은 **2222번** (cluster04는 22번). 외부 5408 → 내부 2222 포워딩
- **1차 논문 데이터**: `~/icngc_pushdown` 6.0 GB 무사

## 노드 사양 — 완벽한 대칭

| | cluster04 (site A) | cluster03 (site B) |
|---|---|---|
| CPU | i9-9900K, 16스레드 | i9-9900K, **16스레드 (동일)** |
| 메모리 | 31 GB | 30 GB |
| Java | OpenJDK 17 | OpenJDK 17 (동일) |
| 내부 IP | 172.29.54.9 | 172.29.54.8 |
| SSH | 5409 → 22 | 5408 → **2222** |
| 홈 여유 | 1.4 TB | 1.4 TB |

## 링크 품질 (cluster04 → cluster03)

| | 값 |
|---|---|
| RTT | **0.191 ms** (10GbE 직결) |
| 대역폭 | **662.9 MB/s** (SSH 암호화 포함) |
| 패킷 손실 | 0% |

미니 PC(16 MB/s, RTT 0.8ms)와 비교해 **41배 대역폭, 4배 낮은 지연**.

## 실험 목적 — 논문의 최대 한계 제거

지금 논문 §5:
> "빠진 것은 네트워크 거리이고, 그건 D를 키우므로 최적점을 위로 민다.
>  우리 수치는 낙관적인 쪽이다."

**2노드에서 D가 실제로 커지면 이 한계가 검증된 예측으로 바뀝니다.**

## 구성 계획

```
cluster04 (site A)              cluster03 (site B)
─────────────────              ─────────────────
JobManager                     TaskManager × N
TaskManager × N                (상태 복원 시 노드 간 전송 발생)
Kafka 브로커
생성기
```

핵심: TaskManager를 두 노드에 나눠, 장애 복구 시 **상태가 노드 경계를 넘어
전송**되게 함. 그래야 D에 네트워크 거리가 더해짐.

## 필요한 설정 변경

| 파일 | 변경 |
|---|---|
| `config.yaml` | `jobmanager.rpc.address`, `bind-host` → 172.29.54.9 (localhost 아님) |
| `config.yaml` | `taskmanager.host` → 각 노드 자기 IP |
| cluster03 | Flink 배포 (`~/icngc2_stream/` rsync) + Java 확인(있음) |
| Kafka | `advertised.listeners`를 172.29.54.9로 (cluster03 TM이 접근) |

## 측정 계획 (기준 arm 재측정)

- k200k ρ=0.50, 무장애 블록(δ·ℓ₀) + M=55 블록(D·에피소드)
- τ 격자는 현재 저장소 속도의 포화 하한 위로
- 16~20런, 3~4시간
- **비교 대상**: 1노드에서 잰 D=4.28s(빠른 저장소)/12.25s(느린) → 2노드에서 얼마나 커지나

## 주의

- cluster03도 공용 서버. 코어 고정 + 대조런 삽입 동일 적용
- 저장소 드리프트: 2노드면 쓰기가 두 디스크로 분산 → 드리프트 완화 기대

---

## 2026-09-09 18:06 — 2노드 클러스터 기동·검증 완료

### 구성 완료

```
cluster04 (site A, 172.29.54.9)      cluster03 (site B, 172.29.54.8)
─────────────────────────────       ─────────────────────────────
JobManager (코어 4)                  MinIO :9000 (체크포인트 저장)
Kafka 브로커 (코어 0-3)              TaskManager × 2 (코어 5-13)
TaskManager × 2 (코어 5-13)
생성기 (코어 14-15)
```

- **TaskManager 4개**: cluster04 2개 + cluster03 2개, 총 8슬롯
- **체크포인트 저장소**: cluster03 MinIO (S3, `s3://flink-ckpt/ckpt`)
- **S3 플러그인**: flink-s3-fs-presto-1.20.5 양 노드 설치
- **Kafka advertised.listeners**: 172.29.54.9:9092 (cluster03 TM이 접근)

### 스모크 검증 (2026-09-09 18:06)

- 잡 RUNNING, Source·agg·Sink 전부 정상
- **S3 체크포인트 완료 5개, 실패 0** — 크기 420~757MB가 cluster03 MinIO에 저장됨
- **이것이 핵심**: 체크포인트가 네트워크 너머에 있으므로, 장애 복구 시 상태를
  네트워크로 다시 읽어야 함 → D에 네트워크 거리가 더해짐 (논문 예측 검증 대상)

### 걸렸던 문제

- `lat-sink`가 로컬 파일에 쓰는데 cluster03 TM에 스케줄되면 경로 없음
  → 양 노드에 `out/`·`results/` 디렉터리 생성으로 해결
  → 분석 시 양 노드 lat.csv 병합 필요 (driver 수정 예정)

### 남은 것

- driver를 2노드용으로 수정 (양 노드 lat.csv 병합, S3 체크포인트 경로)
- 기준 arm(k200k ρ=0.50) + 상태크기 100/200/400MB 측정 (6~8시간)
- 1노드 D(4.28s 빠름/12.25s 느림)와 비교

---

## 2026-09-10 07:37 — 2노드 본 스윕 시작 (윈도우 업데이트로 중단 후 재개)

### 중단·복구
- 09-09 밤 윈도우 자동 업데이트로 로컬 PC 종료 → μ 캘리브레이션 중단
- **서버(2노드)는 무사**. 데이터·인프라 전부 보존됨
- 09-10 재접속 후 μ 재측정: **2노드 μ = 116,554 rec/s** (1노드 108,925의 1.07배 — 거의 동일)

### 확인된 것
- 무장애 검증런: 140초 내내 안정, 지연 52ms, 체크포인트 8개 완료·실패 0
  → **warmup 글리치는 우발적, 2노드 인프라 견고**
- **2노드 체크포인트 소요 2.5s (포화하한 5s)** — 1노드 후반(9s/18s)보다 빠름
  cluster03 MinIO + 저장소 2대 분산 효과. 넓은 τ 격자 사용 가능

### 본 스윕 (m2n, 19런, ~1.8시간)
| 블록 | arm | τ | 목적 |
|---|---|---|---|
| 1 | k200k ρ0.5 M=0 | 4,8,16,32,64s | δ·ℓ₀ 기준선 (2노드) |
| 2 | k200k ρ0.5 M=55 | 4,8,16,32,64s | **2노드 D 측정** (1노드 4.28s 대비) |
| 3 | k100k ρ0.5 M=0 | 4,8,16,32,64s | δ 스케일링 |
| 4 | k400k ρ0.5 M=0 | 8,16,32,64s | δ 스케일링 |

- 장애는 cluster03/cluster04 TM을 번갈아 kill → 상태가 노드 넘어 복원
- 전부 setsid 분리 실행 (창·SSH 끊겨도 진행)
- 완료 시 ntfy 알림
