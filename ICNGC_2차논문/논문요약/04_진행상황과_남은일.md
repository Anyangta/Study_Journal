# 진행 상황과 남은 일

*최종 갱신: 2026-09-07*

## 완료된 것

| 항목 | 상태 |
|---|---|
| 실험 인프라 (설치·오케스트레이션·장애 주입·분석·그림·수치 검증) | ✅ |
| 모델 유도 + 실측 검증 | ✅ |
| 영문 초안 §1~§7 | ✅ 2종 (`paper_4page_en.md` 제출용, `paper_draft_en.md` 상세) |
| 그림 | ✅ 5장 (`../figs/`) |
| 수치 검증 스크립트 | ✅ `../verify.py` — 30개 클레임을 원자료에서 재검증 |
| 재현 가이드 | ✅ `../실험_재현.md` |

## 진행 중

**실험 큐 163런** (m2 42 → m3 44 → m4 42 → m5 35), 체인으로 자동 연결,
약 27시간 분량. **2026-09-07 00:07에 깨끗한 상태로 재시작**했습니다.

재시작한 이유는 디스크 드리프트 때문입니다 —
자세한 건 [05_함정과_교훈.md](05_함정과_교훈.md) 4번 참고.
오염된 42런은 지우지 않고 서버의 `results_drift/`에 보존해 뒀습니다
(드리프트 자체를 논문 한계 절에 문서화할 수 있음).

> ⚠️ **현재 이 로컬에서는 서버 접속이 안 됩니다.** 실험은 자율적으로 돌고 있지만
> 상태 확인과 분석을 하려면 아래 "사용자가 해야 할 것" 1번이 필요합니다.

## 남은 일 (우선순위 순)

### 반드시 (투고 조건)

1. **재실행 결과로 전 수치 갱신** → `verify.py` 재통과 확인
   드리프트 수정 후에도 핵심 결과가 재현되는지 확인하는 게 최우선
2. **참고문헌 6~8건 직접 작성** (아래 목록) — AI에게 시키지 말 것
3. **ICNGC 템플릿 변환 + 4쪽 최종 압축**
   현재 4쪽 초안이 3146 단어. IEEE 2단 기준 대략 4쪽이지만 300~400 단어 정도
   더 줄여야 할 수 있음
4. **AI 사용 공개 여부 확인** — ICNGC 정책 확인 후 지도교수와 상의
5. 그림 캡션 작성

### 결과를 강화하는 것

6. **δ·D의 상태 크기 스케일링** (100/200/400/800MB) → fig4 완성
   현재 2점만 있음. τ\* ∝ δ^(2/3) 이므로 "상태가 큰 잡일수록 최적 주기가
   길어진다"는 예측을 확인할 수 있음
7. **MTBF held-out 검증** (150초, 300초)
   지금까지의 검증은 전부 같은 데이터 안에서 이뤄짐. held-out은 모델이 본 적
   없는 조건을 예측하는 것이라 **훨씬 강한 증거**
8. 가동률 0.3/0.65 arm — 최적점이 ρ에 거의 무관하다는 모델 예측 확인
9. incremental·unaligned 체크포인트, hashmap 백엔드, 병렬도 2/6
   → RocksDB 특정 현상이 아님을 보이는 일반성 확인

### 선택

10. cluster03 복구 시 **진짜 2노드 재실험** — 논문의 가장 큰 한계가 사라짐

## 사용자가 직접 해야 할 것

### 1. 이 로컬에서 서버 접속 열기 (지금 막혀 있음)

```
ssh-copy-id -p 5409 wontak@220.149.241.201
```

키는 이미 생성해 뒀습니다. 비밀번호 한 번 입력하면 끝입니다.
이게 없으면 실험 결과 확인·분석이 전혀 불가능합니다.

### 2. cluster03 물리 재부팅

전원 버튼 한 번. ping은 되고 TCP 핸드셰이크도 되는데 유저스페이스 프로세스가
전부 응답하지 않는 상태라 원격 복구가 불가능합니다.

살아나면 진짜 2노드 구성이 되고, 논문의 가장 큰 한계("단일 머신")가 사라집니다.
현재는 한계 절에 정직하게 명시해 뒀습니다 — 네트워크 거리가 빠져 있으니
우리 수치는 **낙관적인 쪽**이라고.

### 3. 참고문헌 직접 작성

초안에 `[REF: …]` 자리표시자로만 남겨 뒀습니다.
**가짜 인용을 만드는 것보다 비워두는 게 낫다고 판단했습니다.**

| 자리표시자 | 필요한 것 |
|---|---|
| `[REF: Young 1974]` | 1차 근사 체크포인트 주기 공식 |
| `[REF: Daly 2006]` | 고차 보정, 보통 인용되는 형태 |
| `[REF: Flink checkpointing]` | asynchronous barrier snapshotting |
| `[REF: Kafka]` | durable 로그, replay 의미론 |
| `[REF: unaligned checkpoints]` | Flink의 해당 메커니즘 |
| `[REF: streaming latency SLOs]` | 종단 지연을 목적함수로 삼는 근거 논문/산업 보고서 1건 |

## 논문 압축 우선순위 (4쪽에 안 들어갈 때)

**살릴 것**
- 최적점 비교표 (고전 공식과의 거리) — 논문의 결론
- 무보정 예측 검증 (R²=0.999) — 가장 강한 증거
- 안정 한계 — 고전 공식이 표현조차 못 하는 것

**줄일 것**
- 주기의 바닥 (§4.1) → 2~3문장
- 낭비 목적함수 실측 (§4.5a) → 2~3문장
- 한계 절 → 3문장

**그림 3장**
1. `fig1_latency_vs_tau` — 지연 vs 주기, 실측점 + 고전 규칙의 선택 표시
2. `fig3_area_validation` — 무보정 예측 검증 (log-log)
3. `fig2_scaling` — 두 목적함수의 최적 주기 vs MTBF + 불안정 영역

> `fig0_trace`(장애 하나의 지연·처리량 시계열)가 메커니즘을 가장 잘 보여주므로,
> 그림이 3장만 들어간다면 2번과 바꾸는 것도 고려할 만합니다.

## 데이터 신뢰성에 대해

**측정 데이터는 전부 실측이고 조작이 없습니다.** `verify.py`로 논문의 모든
수치를 원자료 CSV에서 재검증할 수 있습니다. 실제로 이 스크립트가 초안의 오류
두 개(μ 분산 13.9% → 17%, 상대오차 24.7% → 14.6%)를 잡아냈습니다.

문제가 되는 건 데이터가 아니라 **문장의 저자성**입니다 — 1차 논문 때와 같은
이슈이므로 같은 방식으로 처리하시면 됩니다.
