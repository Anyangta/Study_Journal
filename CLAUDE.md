# Study Journal — Claude Code 지침

이 저장소는 여러 로컬 환경을 오가며 작업하는 사용자의 "세션 간 기억 저장소"입니다.
이 저장소 안에서 작업을 시작하거나, 사용자가 다른 분야/프로젝트의 맥락을 이어가려
할 때 아래 절차를 따르세요.

> **가장 먼저 할 일**: 이 저장소에서 뭔가를 만들거나 고치기 전에는 항상 아래
> "브랜치 처리" 절차부터 실행해서 `origin/main` 최신 상태로 맞추세요. 세션이 어느
> 브랜치에서 시작됐는지는 신경 쓰지 마세요.

## 폴더 구조

```
<폴더>/                                        # 예: ICNGC/
  <폴더>_<파일명>/                              # 예: ICNGC_실험노트/
    <날짜>_<종류>_<제목>_<버전>.md                # 예: 2026-09-04_회의록_킥오프_0.0.1.md
CLAUDE.md                                        # 이 파일
README.md
```

- **폴더**: 최상위 분류 (예: ICNGC, 수학, React 등)
- **파일명**: 그 폴더 안의 하위 묶음 이름. `<폴더>_<파일명>` 형태로 하위폴더가 됩니다.
- **종류/제목**: 하위폴더 안의 실제 문서(.md 파일) 하나를 식별하는 값. 같은 하위폴더
  안에 종류·제목 조합이 다른 문서가 여러 개 있을 수 있습니다.
- **버전**: `MAJOR.MINOR.PATCH` (semver) 형식. 같은 종류·제목 문서를 다시 만들면
  새 파일을 또 만들지 않고 버전만 올려서 파일명을 바꿉니다 (아래 "새 항목 생성"
  참고).

실제 코드는 이 저장소에 커밋하지 않고, 각 프로젝트의 저장소에 남깁니다. 여기에는
"무엇을 했고, 무엇이 남았는지"에 대한 기록만 남깁니다.

### 값 정리(슬러그화) 규칙

폴더/파일명/종류/제목 값은 모두 아래 순서로 정리한 뒤 사용합니다 (구분자 `_`와의
충돌을 막기 위함).

1. 앞뒤 공백 제거
2. 값 안에 원래 `_`가 있으면 `-`로 바꿉니다.
3. 값 안의 띄어쓰기(공백)는 `_`로 바꿉니다.

(순서가 중요합니다: 원래 있던 `_`를 먼저 `-`로 치환해야, 그다음 공백을 `_`로 바꿔도
구분자끼리 섞이지 않습니다.)

## 브랜치 처리 (가장 먼저, 매번)

Claude Code on the web 등 원격 세션은 세션마다 자동으로 새 브랜치(예:
`claude/xxx-yyy-zzzzzz`)를 만들어 그 위에서 작업하는 경우가 많습니다. 게다가
세션이 오래 이어지면 그 사이 `main`이 더 앞서가도, 그 세션은 여전히 자기가 시작한
시점의 낡은 브랜치/규칙을 그대로 쓰고 있을 수 있습니다 (실제로 이 문제로 오래된
규칙을 되살리거나 중복 항목을 만든 적이 있습니다).

그래서 **이 저장소에서 뭔가를 새로 만들거나 고치기 전에, 세션이 끝날 때가 아니라
매번 작업을 시작하기 직전에** 아래를 먼저 실행해서 로컬을 `origin/main` 최신
상태로 맞추세요.

```bash
git fetch origin main
git checkout -B main origin/main
```

- 지금 세션이 어떤 브랜치에서 시작됐든, 이 두 줄로 로컬 `main`이 원격 `main`과
  똑같아지고 그 위에 올라섭니다. 이 시점엔 아직 이 저장소에 아무 작업도 안 했을
  것이므로 안전합니다.
- 이후 작업(폴더/파일 생성·수정)과 커밋은 전부 이 `main` 브랜치 위에서 합니다.
- 만약 지금 읽고 있는 이 `CLAUDE.md` 내용이 방금 받아온 `origin/main`의
  `CLAUDE.md`와 다르다면, **origin/main 쪽 최신 내용을 기준**으로 이후 절차를
  따르세요 (지금 이 문서 자체가 오래된 사본일 수 있습니다).
- 다 만든 뒤에는 `git push origin main`으로 바로 push합니다.
  - 거부되면(그 사이 다른 세션이 먼저 push함) `git fetch origin main` →
    `git merge origin/main`(서로 다른 폴더/파일만 건드렸다면 대개 충돌 없이
    자동 병합됨) → 다시 push, 순서로 재시도하세요.
  - 코드 수준 충돌이 나거나 정말 애매하면 무리해서 밀어붙이지 말고 사용자에게
    상황을 설명하고 물어보세요.

## 새 항목 생성 (트리거: "폴더로 만들어줘 폴더 : <폴더> 파일명 : <파일명> 종류 : <종류> 제목 : <제목>")

사용자가 이 형식(또는 폴더·파일명·종류·제목 네 값을 명확히 알 수 있는 다른 표현)
으로 요청하면 아래 절차를 그대로 수행하세요.

1. **최신 동기화**: 위 "브랜치 처리" 절차대로 `origin/main` 최신 상태로 맞춘
   뒤 시작합니다.
2. **값 정리**: 폴더·파일명·종류·제목 네 값 모두 위 "값 정리(슬러그화) 규칙"을
   적용합니다.
3. **폴더 확인/생성**: 저장소 루트에 `<폴더>/`가 없으면 새로 만듭니다.
4. **하위폴더 확인/생성**: `<폴더>/<폴더>_<파일명>/`이 없으면 새로 만듭니다.
5. **기존 문서 검색**: 그 하위폴더 안에서 파일명이 `*_<종류>_<제목>_*.md` 패턴과
   일치하는 파일(즉 종류·제목이 같은 문서)이 있는지 찾습니다.
   - **없으면 (새 문서)**: 버전 = `0.0.1`. 파일명 =
     `<오늘날짜:YYYY-MM-DD>_<종류>_<제목>_0.0.1.md`로 새로 만듭니다.
   - **있으면 (기존 문서 개정)**: 기존 파일의 버전을 아래 규칙으로 올립니다.
     - 기본값: **PATCH만 +1** (예: `0.0.1` → `0.0.2`). 사용자가 딱히 크기를
       언급하지 않으면 이 기본값을 씁니다.
     - 사용자가 "기능/내용을 추가했다"는 취지로 말하면: **MINOR +1, PATCH는
       0으로 리셋** (예: `0.1.3` → `0.2.0`).
     - 사용자가 "구조/주제 자체가 크게 바뀌었다"는 취지로 말하면: **MAJOR +1,
       MINOR·PATCH는 0으로 리셋** (예: `0.4.2` → `1.0.0`).
     - 기존 파일을 `git mv`로 `<오늘날짜>_<종류>_<제목>_<새버전>.md`로 이름을
       바꾸고 내용을 갱신합니다. 파일을 여러 개 남기지 않습니다 — 이전 버전
       내용은 git 이력(`git log`)에 남습니다.
6. **문서 내용 형식**: 아래 형식을 따릅니다. (새 문서면 "최초 작성일"을 오늘
   날짜로 채우고, 기존 문서 개정이면 "최초 작성일"은 그대로 두고 "이번 버전
   날짜"만 오늘 날짜로 갱신합니다.)

   ```markdown
   # <종류> — <제목> (v<버전>)

   - **최초 작성일**: YYYY-MM-DD
   - **이번 버전 날짜**: YYYY-MM-DD
   - **폴더**: <폴더>
   - **파일명**: <파일명>
   - **종류**: <종류>
   - **제목**: <제목>
   - **버전**: <버전>
   - **관련 프로젝트/저장소**: (있다면)
   - **브랜치**:
   - **작업 환경**: (예: PC A, Claude Code on the web 등)

   ## 한 일

   -

   ## 다음 할 일

   -

   ## 참고할 맥락 / 결정 사항

   -
   ```

7. **커밋 & 푸시**: 변경 사항을 commit하고, 위 "브랜치 처리"의 push 절차대로
   바로 `origin/main`에 push합니다. 커밋 메시지는
   `[<폴더>/<파일명>] <종류> - <제목> (v<버전>)` 형식을 권장합니다.

## 세션 시작 시

1. 위 "브랜치 처리" 절차대로 `origin/main` 최신 상태로 먼저 맞춥니다.
2. 사용자가 이어가려는 폴더/파일명을 알 수 있다면 해당
   `<폴더>/<폴더>_<파일명>/` 안의 `.md` 파일들을 훑어봅니다.
3. 관련된 문서를 읽어 "다음 할 일"과 맥락을 파악한 뒤 작업을 이어갑니다.
4. 어느 폴더/파일명인지 모르겠다면 사용자에게 간단히 물어봅니다.

## 세션 종료 시 (또는 중요한 진행 후)

사용자가 "기록해줘", "저널에 남겨줘", "push 해줘" 등으로 요청하면, 위 "브랜치
처리" 절차대로 먼저 `origin/main`에 맞춘 뒤, 지금 작업 중인 문서를 위 5~6번
절차(기존 문서면 버전 올리고 갱신, 새 문서면 새로 생성)에 따라 갱신하고 commit한
뒤 바로 `origin/main`에 push합니다. 요청받지 않았다면 먼저 기록 여부를 물어보고
진행하세요 (자동으로 매번 기록하지 않음).

## 주의사항

- 이 저장소에는 실제 프로젝트 코드를 커밋하지 않습니다.
- 기록은 다른 사람이 아니라 미래의 Claude Code 세션(그리고 사용자)이 빠르게 맥락을
  파악하기 위한 것이므로, 장황한 설명보다 핵심 정보 위주로 간결하게 작성합니다.
- 여러 로컬에서 거의 동시에 같은 폴더/파일명/종류/제목으로 항목을 만들거나
  개정하면 버전이 겹칠 수 있습니다. 항상 생성 전에 pull하고, push 시 충돌이 나면
  다시 pull 후 버전을 재계산해서 파일명을 조정하세요.
