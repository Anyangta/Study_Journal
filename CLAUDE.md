# Study Journal — Claude Code 지침

이 저장소는 여러 로컬 환경을 오가며 작업하는 사용자의 "세션 간 기억 저장소"입니다.
이 저장소 안에서 작업을 시작하거나, 사용자가 다른 분야/프로젝트의 맥락을 이어가려
할 때 아래 절차를 따르세요.

## 폴더 구조

```
<분야>/                                   # 예: 수학/, React/, 알고리즘/
  <날짜>_<분야>_<제목>_<버전>/              # 예: 2026-09-04_수학_적분법_0/
    README.md                             # 그 항목의 실제 기록
templates/
  entry-template.md                       # README.md 작성용 템플릿
CLAUDE.md                                  # 이 파일
README.md
```

분야 폴더 이름과 항목 폴더 이름은 사용자가 부여한 "분야"/"제목" 값 그대로를
기준으로 삼습니다. 실제 코드는 이 저장소에 커밋하지 않고, 각 프로젝트의 저장소에
남깁니다. 여기에는 "무엇을 했고, 무엇이 남았는지"에 대한 기록만 남깁니다.

## 새 항목 생성 (트리거: "폴더로 만들줘 분야:<분야> 제목:<제목>")

사용자가 이 형식(또는 분야·제목 값을 명확히 알 수 있는 다른 표현)으로 요청하면
아래 절차를 그대로 수행하세요.

1. **최신 동기화**: 작업 전 현재 브랜치를 기준으로 `git pull`해서 원격의 최신
   상태를 받아옵니다. 다른 로컬에서 만든 폴더/버전과 충돌하지 않기 위함입니다.
2. **값 정리**: 분야·제목 값의 앞뒤 공백을 제거합니다. 값 내부에 공백이나 `_`가
   있으면 `-`로 바꿉니다 (폴더명 구분자 `_`와 충돌 방지).
3. **분야 폴더 확인/생성**: 저장소 루트에 `<분야>/` 폴더가 없으면 새로 만듭니다.
4. **버전 계산**: `<분야>/` 폴더 안에서 이름이 `*_<분야>_<제목>_숫자` 패턴과
   일치하는 항목(폴더)을 찾습니다.
   - 일치하는 게 없으면 버전 = `0`
   - 있으면 그중 가장 큰 숫자 + 1
5. **폴더 생성**: `<분야>/<오늘날짜:YYYY-MM-DD>_<분야>_<제목>_<버전>/` 폴더를
   만듭니다. (예: `수학/2026-09-04_수학_적분법_0/`. 같은 날 같은 제목으로 또
   요청하면 `수학/2026-09-04_수학_적분법_1/`처럼 버전이 올라감.)
6. **기록 파일 생성**: 그 폴더 안에 `templates/entry-template.md`를 참고해
   `README.md`를 작성합니다.
7. **커밋 & 푸시**: 변경 사항을 commit하고 push합니다. 커밋 메시지는
   `[<분야>] <제목> (v<버전>)` 형식을 권장합니다.

폴더를 막 만든 시점에는 아직 실제 작업 내용이 없을 수 있습니다. 사용자가 이어서
"이 폴더에 오늘 작업 내용 기록해줘"처럼 요청하면, 새 폴더를 또 만들지 말고 같은
폴더의 `README.md`를 갱신한 뒤 다시 commit/push합니다. 새 폴더(버전 증가)는
사용자가 명시적으로 새 항목 생성을 요청했을 때만 만듭니다.

## 세션 시작 시

1. 사용자가 이어가려는 분야를 알 수 있다면 해당 `<분야>/` 폴더 안의 항목들
   (폴더명 = 날짜_분야_제목_버전)을 날짜순으로 훑어봅니다.
2. 관련된 가장 최근 항목의 `README.md`를 읽어 "다음 할 일"과 맥락을 파악한 뒤
   작업을 이어갑니다.
3. 분야를 모르겠다면 사용자에게 간단히 물어봅니다.

## 세션 종료 시 (또는 중요한 진행 후)

사용자가 "기록해줘", "저널에 남겨줘", "push 해줘" 등으로 요청하면, 지금 작업 중인
항목 폴더의 `README.md`를 갱신하고 commit/push합니다. 요청받지 않았다면 먼저 기록
여부를 물어보고 진행하세요 (자동으로 매번 기록하지 않음).

## 주의사항

- 이 저장소에는 실제 프로젝트 코드를 커밋하지 않습니다.
- 기록은 다른 사람이 아니라 미래의 Claude Code 세션(그리고 사용자)이 빠르게 맥락을
  파악하기 위한 것이므로, 장황한 설명보다 핵심 정보 위주로 간결하게 작성합니다.
- 여러 로컬에서 거의 동시에 같은 분야/제목으로 새 항목을 만들면 버전 번호가 겹칠
  수 있습니다. 항상 생성 전에 pull하고, push 시 충돌이 나면 다시 pull 후 버전을
  재계산해서 폴더명을 조정하세요.
