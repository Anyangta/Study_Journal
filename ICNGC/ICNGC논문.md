# ICNGC / ICNGC논문

## 2026-09-06 — 정책비교와-클라우드실측-추가 (v1.3.0)

### 배경

사용자가 "지금 상태로 ICNGC accept가 될까" 하고 물었고, 검토 결과 **실험량이
아니라 두 가지가 빠져 있다**고 판단했다.
- **비교 대상 부재**: "우리가 X를 N% 이겼다"는 문장이 논문에 한 줄도 없었음
- **제목이 cross-cloud인데 클라우드가 하나도 없음**

둘 다 채웠다. 비용은 0원(공개 HTTPS만 사용, 계정·인증·requester-pays 없음).

### 사건: cluster03(사이트 B) 응답 불능

작업 재개 시 외부 포트 5408이 닫혀 있었고, cluster04를 통해 내부망에서 확인하니
**TCP는 연결되는데 sshd·MinIO·에이전트가 전부 무응답**이었다 (커널은 SYN을
받지만 사용자 공간 프로세스가 응답 안 함 = hang 증상). SSH가 안 되어 진단·복구
불가.
- **데이터는 안전**: `~/icngc_pushdown/`의 synth 1.87GiB, lineitem 1.10GB 그대로
- **9명이 쓰는 공용 서버라 관리자(`sslab`/`selee`)에게 알릴 것**
- 우리 실험이 원인일 가능성은 배제 못 하나, 작업 종료 시점 부하 0.37 / 디스크 1.4T 여유였음
- **cluster04는 정상**이라 이후 작업은 전부 거기서 수행

### B. 실제 클라우드 경로 실측 (cluster04에서, 무료)

`measure_cloud.sh`, 결과 `cloud_validation.md`

| 리전 | RTT | | 경로 | goodput |
|---|---|---|---|---|
| 서울 | 4.9 ms | | CDN 경유 (공개 Parquet) | 93.6 MB/s |
| 도쿄 | 30.1 ms | | CDN 경유 (gharchive) | 73.5 MB/s |
| 오레곤 | 118.0 ms | | **미국 원본 직접 (GCS)** | **28.5 MB/s** |
| 버지니아 | 171.7 ms | | **오레곤 원본 직접 (S3)** | **20.5 MB/s** |
| 아일랜드 | 261.4 ms | | | |

**우리 에뮬레이션 조건이 실측 범위를 정확히 감싼다.**
- `1gbit_50ms` 91.3 MB/s ≈ CDN 경유 93.6 MB/s
- `1gbit_150ms` 33.0 MB/s ≈ 미국 원본 직접 20.5~28.5 MB/s (RTT도 118~172ms 사이)
- 양 끝(11.9 / 888)은 여유 있게 바깥

**부수 발견 (논문 도입부에 넣음)**: 같은 객체를 CDN 엣지에서 받으면 원본
리전보다 3~4배 빠르지만 **egress는 바이트당으로 동일 과금**된다. 즉 "요금 기준
k\*는 불변, 지연 기준만 움직인다"는 우리 주장이 실제 공개 클라우드에서 그대로
관측된다. 에뮬레이션 인공물이 아니라는 근거.

### A. 정책 비교 — 논문에 "무엇을 이겼는지"가 생김

`policy_eval.py`, 결과 `policy_results.txt`

**핵심 프레이밍**: 앞으로 몇 번 조회할지 모르는 상태의 "계속 빌릴까 사버릴까"는
고전 **ski-rental 문제**다. 우리 footer 플래너의 역할은 그 온라인 정책이
필요로 하는 손익분기점 k\*를 **데이터 스캔 없이** 제공하는 것.

무셰이핑 10G, 객체 2000개(재사용 횟수 Zipf), 저장 $0.023/GB-월 포함:

| 정책 | 지연(s) | egress | 저장/월 | 합계 | 복제 객체 |
|---|---|---|---|---|---|
| 항상 푸시다운 (벤더 기본) | 331,336 | $4104.91 | — | **$4104.91** | 0 |
| 항상 복제 | 87,600 | $84.08 | $21.49 | $105.57 | **2000** |
| **손익분기 전환 (우리)** | 86,771 | $78.17 | $7.53 | **$85.70** | **701** |
| 오라클 | 84,539 | $45.07 | $7.53 | $52.60 | 701 |

- 벤더 기본 대비 **비용 48배, 지연 3.8배 절감**
- "항상 복제"와 **지연은 동일한데 복제는 35%만** (2000→701, 저장 65% 절감)
- 오라클 대비 지연 1.03배(10G) / 1.06 / 1.16 / 1.15배 — ski-rental 상한 2.0 이내
- 비용은 오라클의 1.47~1.63배. **결정 자체는 오라클과 동일**(701=701)하고,
  차이는 전환 전까지 버티며 낸 egress. 즉 k를 미리 모르는 대가

### 논문 구조 변경

§6 "Putting k\* to work: a break-even policy" 신설, 기존 결론이 §7로.
기여 목록에 4번(정책) 추가. Abstract에 클라우드 실측과 정책 결과 반영.

### 다음 할 일

1. §2 인용 6~8건 (ski-rental 고전 논문 추가 필요 — Karlin et al.)
2. **ICNGC 템플릿 변환 + 4장 압축.** Result 9개 + §6까지 있어 분량 크게 초과.
   유지 우선순위: §6 정책 비교 > 클라우드 실측 > Result 1(불변량) >
   Result 7(저선택도 집중) > Result 4(RTT). 나머지는 문장으로 축약
3. 그림: 정책 비교 막대그래프를 새로 만들면 §6이 강해짐
4. cluster03 복구 후: 필요하면 추가 측정
5. **정리 (사용자 직접, root 필요)**: cluster03 복구 뒤
   `su -` → `rm /etc/sudoers.d/icngc-tc`


## 2026-09-05 — TPCH전체스윕-세션종료 (v1.2.0)

### 한 일

- **TPC-H도 7개 선택도 전부에서 측정** (`tpch_full`, 63 trial). `analyze6.py`
- **최종 데이터: 513 trial, 9개 라벨, 2개 데이터셋** (`results_final.csv`)
- 논문에 Result 8a 추가. 초고 최종본 `paper_draft.md`

### 결과 — 저선택도 집중 패턴이 두 번째 데이터셋에서도 재현 (조건부)

| σ | synth 지연/요금 (괴리) | TPC-H 지연/요금 (괴리) |
|---|---|---|
| 0.001 | 139 / 796 (6배) | 108 / 58633 (545배) |
| 0.01 | 62 / 94 (2배) | 62 / 692 (11배) |
| 0.05 | 19 / 20 (1배) | never / 40 (불안정) |
| 0.1 | 11 / 10 (1배) | 191 / 14 (역전) |
| 0.25 | 5 / 4 (1배) | 8 / 4 (1배) |
| 1.0 | 1 / 1 (1배) | 2 / 1 (1배) |

- **양 끝단은 재현됨**: 저선택도에서 괴리 크고, σ≥0.25에서 사라짐
- **중간 구간(σ=0.05~0.1)은 불안정.** 그 구간에서 푸시다운과 로컬 스캔의
  질의당 비용이 서로 가까워 k\*의 분모가 작아지고, 측정 노이즈와 추정기
  오차(TPC-H 0.0224)가 증폭됨. **양 끝단만 주장하고 중간은 주장하지 말 것**

### 세션 최종 상태

**산출물** (로컬 `바탕 화면/icngc_pushdown/`, 서버에도 동일)
- `results_final.csv` — 513 trial / 9 라벨 / 2 데이터셋
- `paper_draft.md` — 결과 9개 절(1~8a), placeholder 0개, 전 수치 실측
- 그림: `fig1_crossover`, `fig2_objective_gap`, `fig3_kstar_cores`, `fig4_rtt`
- 분석: `analyze2`(매칭) `analyze3`(모델비교) `analyze4`(7점) `analyze5`(가지치기)
  `analyze6`(두 데이터셋). **`analyze.py`는 σ를 섞으므로 쓰지 말 것**
- 파생 CSV: `kstar_matched`, `kstar_models`, `kstar_v4`, `kstar_accuracy`

**서버** (그대로 켜둠, 재기동 불필요)
- B(cluster03): MinIO :9000, 실행 에이전트 :8800,
  데이터 `synth.parquet` 1.87GiB + `lineitem.parquet` 1.10GB
- netem 원복 확인 (`mq` 상태), 워치독 종료

### 다음 세션 할 일 (우선순위 순)

1. §2 인용 6~8건 채우기
2. **ICNGC 템플릿 변환 + 4장 압축.** Result가 9개라 반드시 취사선택.
   - 유지: Result 1(요금 k\* 링크 불변량), 4(RTT), 6(가지치기 모델 12.5%),
     7(괴리가 저선택도에 집중), 8(TPC-H 추정기 한계)
   - 축소·삭제: Result 2·3(괴리 비단조·방향), 5(코어 음성결과 → 한 줄),
     8a(중간 불안정 → limitations로)
3. 그림 캡션. fig3(코어)은 빼고 fig4(RTT)를 살릴 것
4. 선택: 사이트별 고정 바닥을 넣은 모델 v6 (로컬 ~4.6ms, 원격 ~27ms)
5. 선택: 전체 선택도 스윕을 셰이핑 조건에도 확대
6. **정리 (사용자 직접, root 필요)**: `ssh -p 5408 wontak@220.149.241.201` →
   `su -` → `rm /etc/sudoers.d/icngc-tc`

## 2026-09-05 — 모델개선-가지치기반영 (v1.1.5)

### 한 일

- v1.1.4에서 1순위 과제로 남긴 **비용 모델의 zone map 가지치기 반영을 완료.**
  `analyze5.py`, 결과 `kstar_v4.csv`

### 결과 — 중앙 오차 절반으로 (매칭 100개 구성)

| 모델 | 중앙 오차 | 평균 | 유한 예측 |
|---|---|---|---|
| v1 (가지치기 없음) | 23.4% | 62.0% | 89 |
| **v4 (양쪽 사이트 가지치기)** | **12.5%** | 572% | **100** |
| v5 (v4 + 공통 고정 바닥) | 13.8% | 574% | 100 |

**핵심 비대칭**: 술어가 있는 스캔만 가지치기된다.
- `FULL_PUSH`의 매 질의 → 술어 있음 → `σ` 곱함
- 사이트 A의 복제본 질의 → 술어 있음 → `σ` 곱함
- `PROJECT_ONLY`의 준비 단계 → **술어 없음** → 전체 읽음, `σ` 곱하지 않음

이 비대칭을 반영하니 중앙 오차가 23.4% → 12.5%로 절반이 되고, 89개에서만
가능하던 유한 예측이 100개 전부로 늘었다.

### 남은 한계 (다음 세션이 이어받을 지점)

- v4는 **저선택도에서 과대추정**한다 (σ=0.001에서 예측 1311 대 실측 139).
  가지치기가 스캔을 0으로 만들 수는 없고 사이트별 고정 바닥이 있기 때문
  - 로컬 스캔: 모델 0.19ms 예측, 실측 4.8ms → 약 **4.6ms 바닥**
  - B의 스캔: 모델 0.2ms 예측, 실측 27.4ms → 약 **27ms 바닥** (RTT 12.2ms 포함)
- v5처럼 **공통** 바닥을 더하면 k\*의 분자·분모에 모두 들어가 상쇄되어 무의미
  (13.8%로 오히려 나빠짐)
- **사이트별로 다른 바닥값**을 쓰면 닫힐 가능성이 높지만, 100개 점에 파라미터를
  더 맞추는 건 과적합 위험이라 여기서 멈춤. 다음 세션이 판단할 것
- 평균 오차 572%는 저선택도 극단값 탓. 중앙값이 읽어야 할 통계

### 다음 세션 할 일 (우선순위 순)

1. §2 인용 6~8건 채우기
2. **ICNGC 템플릿 변환 + 4장 압축.** Result가 8개라 반드시 취사선택 필요.
   유지 권장: Result 1(요금 k\* 링크 불변량), 4(RTT), 6(가지치기 모델),
   7(괴리가 저선택도에 집중), 8(TPC-H 추정기 한계).
   줄이거나 뺄 것: Result 2·3(괴리 비단조·방향), 5(코어 음성결과 → 문장 한 줄)
3. 그림 캡션. fig3(코어)은 빼고 fig4(RTT)를 살리는 편이 나음
4. 선택: 사이트별 고정 바닥을 넣은 v6 시도
5. 선택: 전체 선택도 스윕을 셰이핑 조건에도 확대
6. **정리 (사용자 직접, root 필요)**: `ssh -p 5408 wontak@220.149.241.201` →
   `su -` → `rm /etc/sudoers.d/icngc-tc`

## 2026-09-05 — 전체선택도스윕-모델진단 (v1.1.4)

### 한 일

- **무셰이핑 10G 조건을 7개 선택도 전부에서 재측정** (`10gbit_full`, 63 trial).
  기존에는 복제 플랜을 σ ∈ {0.001, 0.1, 1.0} 세 점에서만 재서 매칭 비교가
  세 점뿐이었음. 이제 일곱 점 전부 비교 가능. `analyze4.py`
- 총 450 trial, 8개 라벨 (`results_final.csv`)
- 논문에 Result 7 추가 (기존 TPC-H 절은 Result 8로 밀림)

### 결과 1 — 괴리는 저선택도에 집중된다 (새 발견)

| σ | 푸시다운/질의 | 로컬/질의 | 지연 k\* | 요금 k\* | 괴리 | 나이브 예측 |
|---|---|---|---|---|---|---|
| 0.001 | 27.4ms | 4.8ms | 139 | 796 | 6배 | 25 |
| 0.01 | 64.8ms | 12.0ms | 62 | 94 | 2배 | 22 |
| 0.05 | 215.4ms | 46.5ms | 19 | 20 | 1배 | 13 |
| 0.1 | 380.1ms | 89.1ms | 11 | 10 | 1배 | 9 |
| 0.25 | 871.8ms | 213.6ms | 5 | 4 | 1배 | 5 |
| 0.5 | 1654ms | 447.4ms | 3 | 2 | 1배 | 2 |
| 1.0 | 3196ms | 748.8ms | 1 | 1 | 1배 | 1 |

**σ ≥ 0.05에서는 두 목적함수가 사실상 같은 답을 낸다.** 목적함수 선택이
중요한 건 고선택도 질의(결과가 작아 푸시다운의 egress 이점이 큰 경우)뿐이다.
세 점만 봤을 때는 안 보이던 구조.

### 결과 2 — 모델의 남은 오차 원인을 특정함

- 로컬 복제본 스캔 시간이 σ에 거의 정비례 (4.8 → 748.8ms).
  **B의 스캔도 마찬가지**: σ=0.001에서 실측 27.4ms인데 모델은 약 200ms 예측
- 원인: **양쪽 사이트 모두 zone map으로 row group을 건너뛰는데, 비용 모델은
  어느 쪽 스캔도 가지치기 가능하다고 보지 않는다.** 우리 플래너가 footer에서
  읽는 바로 그 통계를 DuckDB도 쓰고 있음
- 7개 매칭 지점 나이브 모델 중앙 상대오차 17.5%, 잔차가 저선택도에 집중 —
  위 누락으로 정확히 설명됨

### 다음 세션 할 일 (우선순위 순)

1. **비용 모델의 스캔 항에 zone map 가지치기를 양쪽 사이트 모두 반영.**
   `planner.components()`의 `scan_b`와 로컬 스캔 항에 σ를 곱하는 형태.
   Result 7이 잔차가 여기 있다고 지목함. 가장 값어치 있는 한 수
2. §2 인용 6~8건
3. ICNGC 템플릿 변환 + 4장 압축 (현재 크게 초과, Result가 8개라 취사선택 필요.
   핵심은 Result 1(요금 k\* 불변량), 4(RTT), 7(저선택도 집중), 8(추정기 한계))
4. 그림 캡션. fig3(코어 음성결과)은 문장으로 줄여 공간 확보 권장
5. 전체 선택도 스윕을 셰이핑 조건에도 확대 (조건당 약 20분~1시간)
6. **정리 (사용자 직접, root 필요)**: `ssh -p 5408 wontak@220.149.241.201` →
   `su -` → `rm /etc/sudoers.d/icngc-tc`

### 이번 세션 최종 산출물

- `results_final.csv` — 450 trial, 8개 라벨, 2개 데이터셋
- `paper_draft.md` — 결과 8개 절, placeholder 0개, 모든 수치 실측
- 그림 4종 (pdf+png), `kstar_matched.csv`, `kstar_models.csv`, `kstar_accuracy.csv`
- 분석 스크립트 `analyze.py`(σ혼합, 사용 금지) / `analyze2.py`(매칭) /
  `analyze3.py`(모델 비교) / `analyze4.py`(7점 매칭)

## 2026-09-05 — 모델정확도-매칭재계산 (v1.1.3)

### 한 일

- **v1.1.2에서 "잠정치"로 남겨둔 모델 정확도를 선택도 매칭 기준으로 재계산.**
  `analyze3.py`, 결과 `kstar_models.csv`
- 재계산 과정에서 모델의 또 다른 오류를 발견하고 v3에 반영:
  **로컬 복제본 스캔 비용도 선택도에 비례한다.** DuckDB가 복제본에서도 우리가
  footer에서 읽는 것과 같은 zone map으로 row group을 건너뛰기 때문.
  실측 4.7ms(σ=0.001) → 88.5ms(σ=0.1) → 657ms(σ=1)

### 결과 — 보정할수록 나빠짐 (음성 결과)

선택도 매칭 72개 구성 기준.

| 모델 | 중앙 상대오차 | 평균 | 유한 예측 수 |
|---|---|---|---|
| 나이브 (스캔+전송만) | **9.3%** | 98.8% | 65 |
| v2 (+요청당 오버헤드 12.2ms, +직렬화 259MB/s) | 15.9% | 78.1% | 68 |
| v3 (+복제본 zone map 가지치기) | 16.1% | 86.3% | 72 |

- **측정 기반 보정항을 넣을수록 중앙 오차가 커진다.** 나이브 모델이 가장 정확
- 커버리지는 반대 방향: 나이브는 65개에서만 유한 예측, v3는 72개 전부
- 평균 오차가 78~99%로 큰 것은 소수 극단값 탓. 중앙값이 읽어야 할 통계
- v1.1.1에 적은 "나이브 13.6% → 정밀 10.5% 개선"은 **σ 혼합 기준의 값이며
  매칭 기준으로는 개선이 아니라 악화**. 논문 Result 6을 이 내용으로 교체함
- 결론: 4장짜리 논문에서는 나이브 모델로 충분하다고 쓰고, 정교화 시도가
  실패했다는 사실도 함께 보고 (k를 임계값의 어느 쪽에 놓을지만 맞으면 되는 과제)

### 다음 세션 할 일 (v1.1.2 목록에서 1번 완료됨)

1. §2 인용 6~8건 채우기
2. ICNGC 템플릿 변환 + 4장 압축 (현재 초과). Fig 3(음성 결과)을 문장으로 줄이면
   공간 확보 가능
3. 그림 캡션 작성
4. 선택: `run_all.sh`의 `FLAT`을 `SIGMAS`와 같게 재실행하면 매칭 지점이 3→7개
   (약 1시간). 지금은 σ ∈ {0.001, 0.1, 1.0} 세 점만 매칭 비교 가능
5. **정리 (사용자 직접, root 필요)**: `ssh -p 5408 wontak@220.149.241.201` →
   `su -` → `rm /etc/sudoers.d/icngc-tc`

### 이번 세션에서 두 번 반복된 교훈

측정은 멀쩡했는데 **분석의 암묵적 가정이 두 번 결과를 뒤집었다.**
(1) 복제 플랜의 질의당 시간이 σ와 무관하다는 가정 → "never replicate" 허상,
(2) 로컬 스캔에 가지치기가 없다는 가정 → 모델 v1의 구조적 편향.
둘 다 "바이트는 σ와 무관"이라는 참인 명제를 시간으로 잘못 옮긴 것.
다음에 비슷한 모델을 세울 때 **양(bytes)과 시간(seconds)의 의존성을 항상
따로 따질 것.**

## 2026-09-05 — 세션종료-초고완성 (v1.1.2)

### 한 일

- 논문 초고 `paper_draft.md` 완성 (263줄, 결과 7개 절, placeholder 0개).
  v1.1.1의 정정을 전부 반영했고, 철회한 "never replicate" 주장은 제거함
- 그림 4장 최종본: `fig1_crossover`, `fig2_objective_gap`, `fig3_kstar_cores`,
  `fig4_rtt` (pdf+png). fig4는 선택도 매칭 데이터로 재생성함
- 서버 정리 완료: 워치독 종료, netem 원복(`mq` 상태 확인), MinIO(:9000)와
  실행 에이전트(:8800)는 다음 세션이 바로 쓰도록 **켜둔 채 유지**

### 논문 초고 구성 (paper_draft.md)

- §3 단일 질의 결과와 축 전환의 근거 (스캔 336~1374 MB/s 대 WAN 12.5~125 MB/s)
- §4 세 플랜의 비용 모델, footer 기반 σ̂·π̂ 추정
- §5 결과 7개
  1. 요금 기준 k\*는 링크 불변량 (796 / 10 / 1, 6개 조건 동일)
  2. 괴리는 대역폭에 비단조, 중간에서 최대 (5배 → 30배 → 7배)
  3. 어느 목적함수가 더 보수적인지가 링크에 따라 갈림 (크기는 작음, 과장 금지)
  4. RTT만으로 k\*가 6배 이동 (174→69→31→27)
  5. 음성 결과: B의 코어 수는 무의미
  6. 모델 정확도 나이브 13.6% / 정밀 10.5% (**잠정치 — 매칭 기준 재계산 필요**)
  7. TPC-H 추가, 추정기가 깨지는 지점을 정량화 (오차 0.0224, 9배 과대추정)

### 다음 세션이 바로 할 일 (우선순위 순)

1. **모델 정확도 재계산.** `analyze.py`의 `meas()`가 복제 플랜 시간을 σ로
   섞는 버그가 남아 있음. `analyze2.py`처럼 매칭해야 함. 13.6%/10.5%는 잠정치
2. §2 인용 6~8건 채우기
3. ICNGC 템플릿 변환 + 4장 압축 (현재 분량 초과). Fig 3(음성 결과)을 문장으로
   줄이면 공간 확보 가능
4. 그림 캡션 작성
5. 선택: `run_all.sh`의 `FLAT`을 `SIGMAS`와 같게 해서 재실행하면 매칭 지점이
   3개에서 7개로 늘어남 (약 1시간)

### 산출물 위치

- 로컬 `바탕 화면/icngc_pushdown/`: 코드 전체, `paper_draft.md`,
  `results_final.csv`(387 trial), `kstar_matched.csv`, `kstar_accuracy.csv`,
  그림 8개(pdf+png), `calib.json`, `calib2.json`
- 서버 `~/icngc_pushdown/`: 같은 코드 + 데이터(synth 1.87GiB, lineitem 1.10GB)

## 2026-09-05 — 결과정정-선택도매칭-TPCH추가 (v1.1.1)

### 한 일

- **v1.1.0에 기록한 수치 일부를 정정.** 아래 "정정 내용" 참고. 원인은 분석
  오류였고 실험 데이터 자체는 문제 없음
- **TPC-H `lineitem` SF=5 (1.10 GB) 두 번째 데이터셋 추가.** 43 trial.
  총 387 trial, 7개 조건
- 선택도 매칭 분석 스크립트 `analyze2.py` 작성, 결과 `kstar_matched.csv`

### 정정 내용 (중요)

**틀린 주장 — 철회**: "10Gbit에서는 지연 기준 k\*가 never(복제하지 마라)".

원인: 복제 플랜(`PROJECT_ONLY`/`FULL_SHIP`)의 질의당 **바이트**는 선택도와
무관하지만 질의당 **시간**은 무관하지 않다. 결과 행을 materialize하는 비용이
선택도에 비례하기 때문. 그런데 `analyze.py`의 `meas()`가 복제 플랜의 시간을
선택도 전체에 걸쳐 중앙값으로 섞어버렸다 (89ms). 선택도를 맞추면 σ=0.001에서
로컬 스캔은 4.7ms이고, 푸시다운(23.6ms)보다 **빠르다**. 따라서 k\*는
never가 아니라 **174**로 유한하다.

복제 플랜은 σ ∈ {0.001, 0.1, 1.0} 세 점에서만 측정했으므로(run_all.sh의 FLAT),
매칭 비교가 가능한 것도 그 세 점뿐이다. `analyze2.py`가 그 세 점만 다룬다.

### 핵심 결과 (정정 후, σ=0.001·B 4코어 기준)

| 조건 | goodput | 푸시다운/질의 | 로컬/질의 | 복제준비 | 지연 k\* | 요금 k\* | 괴리 |
|---|---|---|---|---|---|---|---|
| 10GbE 무셰이핑 | 888 MB/s | 23.6ms | 4.7ms | 3.28s | 174 | 796 | 5배 |
| 1Gbit+10ms | 118 MB/s | 103.9ms | 4.9ms | 6.82s | 69 | 796 | 12배 |
| 1Gbit+50ms | 91.3 MB/s | 384.4ms | 4.8ms | 11.72s | 31 | 796 | 26배 |
| 1Gbit+150ms | 33.0 MB/s | 1084.6ms | 4.8ms | 28.84s | 27 | 796 | 30배 |
| 500Mbit+50ms | 58.5 MB/s | 385.5ms | 5.1ms | 12.19s | 32 | 796 | 25배 |
| 100Mbit+50ms | 11.9 MB/s | 397.8ms | 5.0ms | 44.85s | 114 | 796 | 7배 |

1. **요금 기준 k\*는 링크 불변량 — 이 발견은 그대로 유효.** 6개 조건 전부에서
   σ=0.001일 때 796, σ=0.1일 때 10, σ=1일 때 1로 **완전히 동일**. 옮기는
   바이트 수는 대역폭·RTT와 무관하므로. 논문의 중심 주장
2. **지연 기준 k\*만 링크에 따라 27~174로 변동.** 괴리는 5~30배,
   1Gbit+150ms에서 최대
3. **괴리는 대역폭에 대해 비단조.** 888MB/s에서 5배 → 33MB/s에서 30배 →
   11.9MB/s에서 다시 7배. 느린 링크에서는 복제 준비 비용(44.85s)이 커져
   지연 k\*도 함께 올라가기 때문
4. **약화된 주장**: "불일치 방향이 뒤집힌다"는 σ=0.1에서 10Gbit(지연12 대 요금10)와
   셰이핑 조건들(지연7~9 대 요금10) 사이에 부호가 갈리지만 크기가 작다.
   과장하지 말 것
5. **음성 결과 (유효)**: B의 코어 수 1/2/4/8은 k\*에 거의 영향 없음.
   DuckDB 1스레드도 링크보다 빨라 원격 CPU가 병목이 아님
6. **모델 정확도**: 나이브 13.6% → 정밀 10.5% (단, 이 값들은 σ 혼합 분석
   기준이므로 매칭 기준으로 재계산 필요 — 다음 세션 할 일)

### TPC-H 결과 (선택도 추정기의 한계를 정량화)

| 데이터셋 | 술어 컬럼 | 최대 절대오차 | 중앙 오차 |
|---|---|---|---|
| synth | `day` (행 순서와 정렬) | 0.0009 | 0.0002 |
| TPC-H | `l_shipdate` (무상관) | 0.0224 | 0.0130 |

zone map이 가지치기를 못 하면 추정기가 선택도를 **체계적으로 과대추정**한다.
목표 0.01에서 추정 0.0092 대 실제 0.0010으로 9배 과대. 결과적으로 푸시다운이
실제보다 비싸 보여 k\*가 과소추정된다. 논문 limitations에 실측으로 넣을 것.

TPC-H의 k\*(10G, σ=0.001): 지연 104, 요금 58633. 요금 쪽이 극단적인 이유는
목표 0.001에서 실제 선택도가 3e-5로 거의 0행이기 때문 — 과대해석하지 말 것.

### 다음 할 일

- **모델 정확도를 선택도 매칭 기준으로 재계산** (`analyze.py`의 `meas()`가
  복제 플랜 시간을 σ로 섞는 문제를 고쳐야 함). 13.6%/10.5%는 잠정치
- 복제 플랜을 7개 선택도 전부에서 측정하면 매칭 지점이 늘어남
  (`run_all.sh`의 `FLAT`을 `SIGMAS`와 같게)
- §2 인용 6~8건, ICNGC 템플릿 변환, 4장 압축, 그림 캡션
- **정리 (사용자 직접, root 필요)**: `ssh -p 5408 wontak@220.149.241.201` →
  `su -` → `rm /etc/sudoers.d/icngc-tc`

### 이번에 배운 것

측정 자체보다 **집계 방식**이 결과를 바꿀 수 있다. 어떤 양이 어떤 변수와
무관한지(바이트는 σ와 무관, 시간은 아님)를 플랜별로 따로 따져야 했다.
"복제 플랜은 σ에 무관하다"는 편의 가정이 바이트에만 맞고 시간에는 틀렸는데,
그 가정이 코드의 `meas()`에 들어가 있어 눈에 안 띄었다.

## 2026-09-05 — 실험완료-6개조건344trial (v1.1.0)

### 한 일

- **실험 전량 완료. 6개 링크 조건, 총 344 trial.** 원본 데이터
  `results_final.csv`, 조건별 정확도 `kstar_accuracy.csv`
  | 조건 | 실측 goodput |
  |---|---|
  | 10GbE 무셰이핑 | 888 MB/s |
  | 1Gbit + 10ms | 216 MB/s |
  | 1Gbit + 50ms | 91.3 MB/s |
  | 1Gbit + 150ms | 33.0 MB/s |
  | 500Mbit + 50ms | 58.5 MB/s |
  | 100Mbit + 50ms | 11.9 MB/s |
- 그림 4장 생성: `fig1_crossover`, `fig2_objective_gap`, `fig3_kstar_cores`,
  `fig4_rtt` (각각 pdf+png)
- 논문 초고 완성 (`paper_draft.md`) — placeholder 0개, 모든 수치 실측
- netem 정상 원복 확인 (인터페이스가 원래 `mq`+`fq_codel` 상태로 복귀)

### 핵심 결과 (논문의 뼈대)

1. **요금 기준 k\*는 링크 불변량.** 6개 조건 전부에서 σ=0.001일 때 796,
   σ=0.01일 때 94로 **완전히 동일**. 옮기는 바이트 수는 대역폭·RTT와 무관하므로
   당연하지만, 이것이 아래 불일치를 구조적으로 만듦
2. **지연 기준 k\*는 never ~ 145로 변동.** RTT가 "복제할지 말지"를 결정하고,
   대역폭이 "몇 번 만에 회수되는지"를 결정 (두 요인이 깔끔히 분리됨)
3. **불일치의 방향이 뒤집힘 (가장 강한 발견).** RTT 0.1ms에서는 지연이
   "복제하지 마라" / 요금이 "94회면 복제하라" → 지연이 더 보수적.
   RTT 150ms에서는 지연이 "19회" / 요금이 "94회" → **요금이 더 보수적**.
   크기 차이가 아니라 어느 쪽이 먼저 복제를 권하는지가 링크에 따라 역전됨
4. **RTT 계열 (1Gbit 고정, σ=0.01)**: 0.1ms→never, 10ms→59, 50ms→24, 150ms→19.
   RTT→0에서 k\*가 발산. 대역폭 교란 없이 검증됨
5. **음성 결과: B의 코어 수는 무의미.** 1/2/4/8 스레드에서 k\*가 사실상 평평
   (σ=0.05에서 29/27/31/32). DuckDB 1스레드도 링크보다 빠르므로 원격 CPU가
   병목이 되지 않음. **원래 주제 D를 잡을 때 핵심 축으로 삼았던 가설이 반증됨**
6. **모델 정확도**: 나이브 13.6% → 정밀 10.5% (측정 기반 보정항 2개 추가:
   요청당 왕복 12.2ms, B의 Parquet 직렬화 259 MB/s)

### 다음 할 일

- **논문 마무리 (09/06~)**
  - §2 인용 6~8건 채우기 (S3 Select, PushdownDB, Skyplane, Globus/GridFTP,
    SPANStore, Parquet zone map)
  - ICNGC 템플릿으로 변환하고 4장으로 압축 (현재 분량 초과)
  - 그림 캡션 작성. Fig 3(음성 결과)을 그림으로 둘지 문장 한 줄로 줄일지 판단
  - 선택: TPC-H `lineitem`으로 두 번째 데이터셋 (이미 생성 가능 상태,
    `python3 gen_data.py --tpch`). 주장 성립에 필수는 아님
- **정리 작업 (사용자 직접, root 필요)**
  `ssh -p 5408 wontak@220.149.241.201` → `su -` → `rm /etc/sudoers.d/icngc-tc`

### 참고할 맥락 / 결정 사항 (다음 세션 이어받기용)

- **서버 접속**: `ssh icngc-b` = cluster03 = 172.29.54.8 = 사이트 B (데이터),
  `ssh icngc-a` = cluster04 = 172.29.54.9 = 사이트 A (분석).
  `~/.ssh/config`에 별칭 등록됨, 키는 `~/.ssh/id_ed25519_icngc` (암호 없음)
  - 주의: 외부 포트는 **5408=cluster03, 5409=cluster04**로 번호와 순서가 반대
  - 내부 sshd는 **2222 포트** (A→B 제어용, A의 키가 B에 등록돼 있음)
- **서버에 켜둔 것** (재기동 불필요): B에 MinIO(:9000, admin/password123)와
  실행 에이전트(:8800). 데이터 `~/icngc_pushdown/data/synth.parquet` 1.87GiB,
  MinIO 버킷 `warehouse`에도 업로드됨
- **코드 위치**: 로컬 `바탕 화면/icngc_pushdown/`, 양 서버 `~/icngc_pushdown/`
  (저장소 규칙상 저널에는 커밋 안 함)
- **실험 재실행**: A에서 `DEADLINE=$(date -d "HH:MM" +%s) setsid nohup
  ./drive_all.sh > drive.log 2>&1 &`. 조건 목록은 `drive_all.sh` 하단
- **오늘 겪은 함정 3가지 (반복하지 말 것)**
  1. **실행 중인 셸 스크립트를 수정하면 안 됨.** bash가 바이트 오프셋으로 읽어가서
     실행이 깨짐. 1gbit_50ms 조건을 한 번 날렸음
  2. **Windows에서 scp한 셸 스크립트는 CRLF 줄바꿈이라 즉시 실패함**
     (`/usr/bin/env: 'bash^M': No such file or directory`). 올린 뒤 반드시
     `dos2unix *.sh` 또는 `sed`로 CR 제거 후 `bash -n`으로 문법 확인할 것
  3. `pkill -f agent_b.py`를 ssh로 실행하면 **자기 명령줄까지 매칭해 세션을 죽임**.
     `pkill -f "[a]gent_b.py"`처럼 써야 함
- **netem은 반드시 포트 대상 지정으로만.** 두 서버는 9명이 쓰는 공용 k8s 노드라
  인터페이스 전체 shaping 금지. `drive_all.sh`의 `shape()`가 u32 필터로
  9000/8800 포트만 잡음. 안전장치로 `setup/watchdog.sh`도 있음
- **DuckDB `.arrow()`는 버전에 따라 RecordBatchReader를 반환**. `as_table()`
  헬퍼로 정규화해뒀음 (`agent_b.py`, `runner.py`)
- **실험 설계 요령**: k를 스윕하지 않음. 한 trial에서 K회 반복해 "일회성 비용"과
  "질의당 비용"을 분리 측정하면 모든 k 곡선이 해석적으로 나옴

## 2026-09-05 — 서버환경확인-netem대상지정 (v1.0.1)

### 한 일

- **서버 환경 확인 완료. 우려했던 문제 없음.**
  - cluster03 / cluster04 = **별개의 물리 머신 2대** (machine-id 상이,
    `systemd-detect-virt: none` 베어메탈). 같은 공인 IP에 포트만 다른 거라
    한 머신 위 컨테이너일까 의심했는데 아니었음
  - CPU: Intel i9-9900K, `nproc`=16 (물리 8코어 / 하이퍼스레딩 16스레드)
  - **직결 LAN**: `enp1s0f0`에서 172.29.54.8 ↔ 172.29.54.9 (같은 /24).
    브리지를 안 거치는 실제 물리 NIC라 netem이 정상 동작함
- 역할 배정 확정
  | | 호스트 | 사설 IP | SSH 포트 | 역할 |
  |---|---|---|---|---|
  | 사이트 B | cluster03 | 172.29.54.8 | 5409 | 데이터 클라우드 (MinIO + 실행 에이전트) |
  | 사이트 A | cluster04 | 172.29.54.9 | 5408 | 분석 클라우드 (`runner.py`) |
- **두 대 모두 살아있는 Kubernetes 노드임을 발견** (`flannel.1`, `cni0`, docker
  컨테이너 다수). 그래서 `setup/netem.sh`를 전면 재작성:
  인터페이스 root qdisc 전체에 걸던 방식 → **실험 포트(9000, 8800)로 나가는
  트래픽만 대상 지정**. `prio` qdisc의 priomap을 전부 0으로 둬서 매칭 안 된
  트래픽은 unshaped band(1:1)로 흘리고, u32 필터로 peer IP + sport 매칭된
  것만 band 1:3의 netem으로 보냄
- `config.py`의 `B_HOST`를 사설 IP 172.29.54.8로 변경
- `setup/measure_net.sh`를 PEER 사설 IP 기반으로 갱신 + load average 출력 추가

### 다음 할 일

- 코드 복사 (Git Bash에서, 계정 `wontak`)
  ```
  cd "/c/Users/Administrator/OneDrive/바탕 화면"
  scp -P 5409 -r icngc_pushdown wontak@220.149.241.201:~/
  scp -P 5408 -r icngc_pushdown wontak@220.149.241.201:~/
  ```
- 사이트 B(5409)에서: `sudo -n true` 확인 / `ss -tlnp | grep -E ':(9000|8800|9001)'` /
  `uptime` / `iperf3 -s` 띄우기
- 사이트 A(5408)에서: `PEER=172.29.54.8 ./setup/measure_net.sh`
- **미확인 3가지 (이 값들이 나와야 다음 단계 확정 가능)**
  1. `ethtool` Speed — 1000Mb/s인지 10000Mb/s인지에 따라 실험 대역폭 2조건이 정해짐
  2. load average — k8s 노드 공유 중이라 부하 높으면 측정 노이즈. 시간대 조정 또는 반복 증가
  3. 9000/8800 포트 충돌 여부 — 쓰이는 중이면 `config.py`와 `netem.sh`의 `PORTS` 동시 변경
- **`sudo`가 안 되면 netem을 못 걸어서 대역폭 조건 실험 자체가 불가.
  그 경우 설계 변경 필요** (링크 1조건 고정 + 코어 축만으로 축소)
- 이후 Day 1 잔여(MinIO 기동 → `gen_data.py` → `agent_b.py`) → 09/06 첫 실험 배치

### 참고할 맥락 / 결정 사항

- 접속: `ssh -p 5409 wontak@220.149.241.201` (cluster03=B),
  `ssh -p 5408 wontak@220.149.241.201` (cluster04=A)
- **실험 트래픽은 반드시 사설 IP(172.29.54.x)를 쓸 것.** 공인 IP
  220.149.241.201 경로는 NAT를 왕복하므로 netem 통제 밖으로 벗어남
- **인터페이스 전체 shaping 금지.** 두 대가 공용 k8s 노드라서 root qdisc에
  netem을 걸면 flannel VXLAN, 다른 사람 워크로드, 본인 SSH까지 같이 느려짐.
  반드시 포트 대상 지정 방식(`netem.sh apply`)을 쓸 것
- `nproc`=16은 하이퍼스레딩 포함 값. 물리 코어는 8개. 실험의 "B 가용 코어"
  축(1/2/4/8)은 DuckDB `SET threads`로 조절하므로 그대로 유효
- 실제 코드는 이 저널에 커밋하지 않음 (저장소 규칙).
  위치: `바탕 화면/icngc_pushdown/`

## 2026-09-05 — 논문주제확정-교차클라우드복제시점 (v1.0.0)

### 한 일

- 논문 주제 확정: **교차 클라우드에서 같은 데이터를 반복 조회할 때, 몇 번째 질의부터
  원격 조회를 멈추고 데이터를 복제해야 하는가(k\* 결정)**.
  제목 후보 — *How Many Queries Before You Move the Data? Latency- and
  Egress-Aware Replication for Cross-Cloud Analytics*
- 후보 A~F 검토 후 D(연산 푸시다운) 선택 → **측정으로 전제가 반증되어** 반복 질의 k 축으로 피벗.
  (자세한 반증 근거는 아래 "결정 사항" 참고)
- 실험 하네스 전체 작성 완료. 위치: `바탕 화면/icngc_pushdown/` (저널 저장소 밖, 커밋 안 함)
  - `agent_b.py`(B의 SQL 실행 에이전트), `gen_data.py`, `planner.py`(footer 기반 추정+비용모델),
    `runner.py`, `run_all.sh`, `plot.py`, `dry_run.py`, `setup/{measure_net,netem,start_minio}.sh`
- 서버 반입 전 오프라인 검증 통과
  - zone-map 선택도 추정기: σ ∈ [0.001, 1.0]에서 최대 절대오차 **0.0008**
  - `plot.py` 전 구간 리허설: 예측 k\* 대 측정 k\* 중앙 상대오차 **2.9%**, 그림 3장 렌더링 확인
- 서버에 올리기 전에 버그 4개 수정
  1. 전송 바이트를 Parquet의 압축 **해제** 크기로 계산 (→ `total_compressed_size`로 수정)
  2. 캘리브레이션이 `SELECT count(*)` — DuckDB가 footer만 읽어 스캔 속도가 측정 안 됨
  3. `runner.py` 재작성이 조용히 실패해 옛 버전이 남아 있던 것
  4. `plot.py`의 `plt.close` 가 주석 처리된 것

### 다음 할 일

- **[미해결, 최우선] 두 서버 간 물리 링크 속도 미확인.** A에서 `setup/measure_net.sh <B_IP>`
  실행 (B에서 `iperf3 -s` 먼저). 이 값으로 목표 대역폭 2조건 확정:
  물리 10G → `{1gbit, 100mbit}`, 물리 1G → `{500mbit, 100mbit}`
- 09/05: B에 MinIO 기동 → `gen_data.py`(파일 ≥ 1 GiB 확인) → `agent_b.py` 상주
- 09/06: `NET_LABEL=1gbit_50ms ./run_all.sh` (~20분).
  fig1 교차점이 생기는지, fig2의 두 곡선이 저선택도에서 갈라지는지 확인
- 09/07: `NET_LABEL=100mbit_50ms ./run_all.sh` (~45분), 여유 있으면 TPC-H 추가
- **09/08: 실험 종료 마감** → 09/11~12 초고, 09/13 완성고, 09/14 포맷/교정, **09/15 투고**
- Intro/Related Work는 실험이 도는 09/06~07에 미리 써둘 것

### 참고할 맥락 / 결정 사항

- **제약**: ICNGC short paper 4장 이내, 마감 09/15, 실험은 09/08까지. 8코어 서버 2대,
  클라우드 미사용, 무료 플랫폼만, 도커 없음(→ MinIO 단일 바이너리로 해결)
- **테스트베드**: A=분석 클라우드, B=데이터 클라우드(MinIO + 실행 에이전트).
  교차 클라우드 WAN은 B의 egress에 `tc netem`으로 에뮬레이션(root 필요).
  egress 요금은 실측이 아니라 공개 단가표(AWS 0.09 USD/GB 등)로 **모델링**.
  논문에는 "WAN-emulated cross-cloud testbed"로 정직하게 기술할 것
- **원래 주제가 깨진 이유 (논문 도입부의 근거로 그대로 쓸 것)**:
  단일 질의에서는 푸시다운이 **어떤 조건에서도** 이긴다. 1스레드 스캔 속도가
  단순합계 1034 / `LIKE` 695 / `regexp_matches` 511 / 정규식+upper 336 MB/s인데,
  WAN은 100Mbit=12.5, 1Gbit=125 MB/s. CPU가 항상 훨씬 빠르므로 교차점이 없다.
  B의 코어를 1개로 줄여도 마찬가지 (0.057s vs 0.832s)
- **그래서 k(반복 질의 횟수) 축으로 전환**. 세 경로:
  `FULL_PUSH`(매 질의마다 egress) / `PROJECT_ONLY`(투영 컬럼만 1회 복제) /
  `FULL_SHIP`(전체 1회 복제). 앞의 둘은 일회성 비용 + k×질의당비용 구조
- **핵심 결과(모델 예측)**: k\*가 σ에 따라 1~4000회로 3자릿수 변동하고,
  **지연 최소화 기준과 egress 요금 최소화 기준의 답이 최대 ~35배 차이**.
  링크가 빠를수록 격차가 커지고(1Gbit σ=0.001에서 27 vs 1000),
  느린 링크에서는 두 기준이 거의 일치(100Mbit에서 206 vs 1000)
- **실험 설계 요령**: k를 스윕하지 않는다. 한 trial에서 K회 반복해 "일회성 비용"과
  "질의당 비용"을 분리 측정하면 모든 k의 곡선이 해석적으로 나온다.
  덕분에 네트워크 조건당 43 trial, 20~45분이면 끝남
- **핵심 기법**: Parquet footer만 읽어서(스캔 없이, ranged GET 몇 번)
  σ̂는 row group zone map(min/max)으로, π̂는 컬럼 청크 압축 크기로 추정
- 실제 코드는 이 저널에 커밋하지 않음. `바탕 화면/icngc_pushdown/README.md`에
  일자별 체크리스트와 "교차점이 안 생길 때의 대응"이 정리돼 있음

## 2026-09-05 — 논문주제찾기 (v0.0.0)

### 한 일

-

### 다음 할 일

-

### 참고할 맥락 / 결정 사항

-
