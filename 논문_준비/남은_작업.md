# 남은 작업

마감 2026-09-15. 우선순위 순.

## 반드시 (투고 조건)

1. **AI 사용 공개 처리** — `투고_전_체크리스트.md` §1, §2 참고. ICNGC 정책 확인 후
   해당 문구 삽입. 지도교수 상의
2. **참고문헌 6~8건 직접 찾아 §2 작성.** AI에게 시키지 말 것 (가짜 인용 위험).
   ski-rental 원전(Karlin 등)은 §6이 그 프레임을 쓰므로 필수
3. **4장으로 압축.** 압축 우선순위는 `투고_전_체크리스트.md` §3
4. **ICNGC 템플릿 변환**
5. **그림 캡션 작성**
6. `verify_claims.py` 재실행 → 34개 통과 확인

## 권장

7. **영문 초안을 본인 문장으로 다시 쓰기.** 최소 Abstract·Intro·Conclusion
8. cluster03 복구 후 관리자에게 상황 공유

## 기술적으로 더 할 수 있는 것 (선택)

논문 성립에는 필요 없습니다. 시간이 남고 서버가 복구되면.

9. **비용 모델 v6: 사이트별 고정 바닥 반영.**
   현재 v4(가지치기 반영)가 중앙 오차 12.5%인데 저선택도에서 과대추정합니다
   (σ=0.001에서 예측 1311 대 실측 139). 원인은 가지치기가 스캔을 0으로 만들 수
   없다는 것이고, 사이트별 바닥이 다릅니다.
   - 로컬 스캔: 모델 0.19ms 예측, 실측 4.8ms → 약 **4.6ms 바닥**
   - B의 스캔: 모델 0.2ms 예측, 실측 27.4ms → 약 **27ms 바닥** (RTT 포함)
   - 공통 바닥(v5)은 k\*의 분자·분모에 모두 들어가 상쇄되어 무의미했음(13.8%)
   - 파라미터 2개를 100개 점에 맞추는 것이라 과적합 주의

10. **전체 선택도 스윕을 셰이핑 조건에도 확대.**
    현재 σ 7점 전부는 무셰이핑(`10gbit_full`, `tpch_full`)에서만.
    셰이핑 조건은 3점(0.001, 0.1, 1.0)이라 매칭 비교 지점이 적음.
    `run_all.sh`의 `FLAT`을 `SIGMAS`와 같게 두고 실행. 조건당 20분~1시간

11. **질의 템플릿 다양화.** 현재 필터-투영 한 종류. 집계나 조인을 넣으면
    일반성 주장이 강해지지만 4장에는 안 들어감

12. **실제 클라우드 계정으로 end-to-end.** 비용이 발생하므로 크레딧이 있을 때만

## 하지 말 것

- AI에게 참고문헌 채워달라고 하기 (가짜 인용)
- `analyze.py` 사용 (선택도를 섞는 초기 버전, `analyze2.py` 이후를 쓸 것)
- 인터페이스 전체에 netem 걸기 (9명이 쓰는 공용 노드)
- 내용을 이해하지 못한 채 투고

## 산출물 위치

| 무엇 | 어디 |
|---|---|
| 영문 초안 | `논문_준비/paper_draft_en.md` |
| 한국어 요약 | `논문_준비/핵심_결과.md` |
| 그림 5장 | `논문_준비/figures/` |
| 클라우드 실측 | `논문_준비/cloud_validation.md` |
| 정책 비교 원자료 | `논문_준비/policy_results.txt` |
| 연구 과정·설계 근거 | `연구_과정/ICNGC_교차클라우드복제.md` |
| 코드·원본 데이터 | 로컬 `바탕 화면/icngc_pushdown/` (저장소 규칙상 미커밋) |
